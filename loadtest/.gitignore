# Generated by run.sh: binary, throwaway databases, k6 output, server logs.
#
# The databases under here hold real plaintext tokens — hundreds of thousands
# of them after a mint run. They are throwaway, but they are still secrets, and
# they must never reach a commit.
results/
